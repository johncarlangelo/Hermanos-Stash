---
description: Add or improve a Hermanos Stash tool using the standard tool architecture
agent: build
---

Read AGENTS.md, TOOL_SPEC.md, ARCHITECTURE.md, DESIGN.md, and VERIFY.md.

Build or improve the requested tool as a complete vertical slice.

Requirements:
- use the shared tool registry;
- follow the standard lifecycle;
- use shared UI components;
- handle loading, success, and error states;
- keep processing outside the renderer where appropriate;
- add meaningful tests;
- integrate history where appropriate;
- verify search/category/tag behavior;
- run the relevant verification suite.

Do not duplicate infrastructure that already exists.

Requested tool:
$ARGUMENTS
